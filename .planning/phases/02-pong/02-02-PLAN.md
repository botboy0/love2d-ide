---
phase: 02-pong
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - projects/01-pong/main.lua
  - projects/01-pong/src/Paddle.lua
  - projects/01-pong/src/Ball.lua
  - projects/01-pong/DEVLOG.md
autonomous: false
requirements:
  - PONG-01
  - PONG-02
  - PONG-03

must_haves:
  truths:
    - "User has read and understood all 13 CS50G Pong versions (pong-0 through pong-12)"
    - "Pong runs from projects/01-pong/ with ball movement, paddle input, collision, and scoring"
    - "Game has state machine with start, serve, play, and done states"
    - "Win condition triggers at 10 points"
    - "Sound effects play on paddle hit, wall hit, and score"
    - "Window resize works via push:resize callback"
    - "DEVLOG.md has entries documenting the rebuild experience"
  artifacts:
    - path: "projects/01-pong/main.lua"
      provides: "Complete game loop with state machine, collision, scoring, audio, resize"
      min_lines: 150
      contains: "gameState"
    - path: "projects/01-pong/src/Ball.lua"
      provides: "Ball class with init, update, collides, reset, render"
      min_lines: 40
      contains: "Ball:collides"
    - path: "projects/01-pong/src/Paddle.lua"
      provides: "Paddle class with init, update, render and boundary clamping"
      min_lines: 30
      contains: "Paddle:update"
    - path: "projects/01-pong/DEVLOG.md"
      provides: "Dev log with entries for versions built"
      min_lines: 50
  key_links:
    - from: "projects/01-pong/main.lua"
      to: "projects/01-pong/lib/push.lua"
      via: "require 'lib/push'"
      pattern: "require.*lib/push"
    - from: "projects/01-pong/main.lua"
      to: "projects/01-pong/lib/class.lua"
      via: "require 'lib/class'"
      pattern: "require.*lib/class"
    - from: "projects/01-pong/main.lua"
      to: "projects/01-pong/src/Ball.lua"
      via: "require 'src/Ball'"
      pattern: "require.*src/Ball"
    - from: "projects/01-pong/main.lua"
      to: "projects/01-pong/src/Paddle.lua"
      via: "require 'src/Paddle'"
      pattern: "require.*src/Paddle"
    - from: "projects/01-pong/main.lua"
      to: "projects/01-pong/assets/fonts/font.ttf"
      via: "love.graphics.newFont"
      pattern: "newFont.*assets/fonts"
    - from: "projects/01-pong/main.lua"
      to: "projects/01-pong/assets/sounds/"
      via: "love.audio.newSource"
      pattern: "newSource.*assets/sounds"
---

<objective>
Guide the user through the incremental Pong rebuild (pong-0 through pong-12), resulting in a complete, playable Pong game with all core mechanics.

Purpose: The user studies each CS50G version, understands the delta, and writes the code themselves. Claude acts as reference and helper — explaining concepts, answering questions, debugging issues. The rebuild IS the learning. By the end, the user has a complete 2-player Pong game they wrote from scratch.

Output: A fully playable Pong game in `projects/01-pong/` with ball physics, paddle input, collision detection, scoring to 10, sound effects, and resize support. DEVLOG.md populated with the user's experience.
</objective>

<execution_context>
@/home/botboy0/.claude/get-shit-done/workflows/execute-plan.md
@/home/botboy0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pong/02-CONTEXT.md
@.planning/phases/02-pong/02-RESEARCH.md
@.planning/phases/02-pong/02-01-SUMMARY.md

@course/01-pong/pong-0/main.lua
@course/01-pong/pong-1/main.lua
@course/01-pong/pong-2/main.lua
@course/01-pong/pong-3/main.lua
@course/01-pong/pong-4/main.lua
@course/01-pong/pong-5/main.lua
@course/01-pong/pong-6/main.lua
@course/01-pong/pong-7/main.lua
@course/01-pong/pong-8/main.lua
@course/01-pong/pong-9/main.lua
@course/01-pong/pong-10/main.lua
@course/01-pong/pong-11/main.lua
@course/01-pong/pong-12/main.lua
@course/01-pong/pong-final/main.lua
@course/01-pong/pong-final/Ball.lua
@course/01-pong/pong-final/Paddle.lua

<interfaces>
<!-- The user will need these require path conventions (from 02-RESEARCH.md) -->
<!-- CS50G uses flat requires; template uses subdirectory paths -->

Require path adaptation:
```lua
-- Template paths (NOT CS50G flat paths):
push = require 'lib/push'
Class = require 'lib/class'
require 'src/Paddle'
require 'src/Ball'

-- Asset paths:
love.graphics.newFont('assets/fonts/font.ttf', 8)
love.audio.newSource('assets/sounds/paddle_hit.wav', 'static')
```

Virtual resolution constants:
```lua
VIRTUAL_WIDTH = 432
VIRTUAL_HEIGHT = 243
WINDOW_WIDTH = 1280
WINDOW_HEIGHT = 720
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Present the incremental rebuild guide and assist user through pong-0 to pong-12</name>
  <files>
    projects/01-pong/main.lua
    projects/01-pong/src/Paddle.lua
    projects/01-pong/src/Ball.lua
    projects/01-pong/DEVLOG.md
  </files>
  <action>
    THIS IS A USER-DRIVEN CODING TASK. Claude does NOT write the game code. Claude's role:

    1. **Present the version progression.** Show the user which version to work on next and what concept it introduces. Use the 13-version table from RESEARCH.md:
       - pong-0: Bare window (love.load, love.draw, love.graphics.printf)
       - pong-1: push.lua virtual resolution; nearest-neighbor filter
       - pong-2: Drawing shapes; font loading
       - pong-3: Keyboard input (love.keyboard.isDown); paddle movement
       - pong-4: Ball as position+velocity; dt-based movement; math.randomseed
       - pong-5: class.lua OOP; Ball and Paddle classes extracted to src/
       - pong-6: FPS display; love.graphics.setColor
       - pong-7: AABB collision; dx reversal; dy randomization
       - pong-8: Score variables; love.keypressed; state machine ('start'/'play')
       - pong-9: 'serve' state; servingPlayer; ball reset
       - pong-10: 'done' state; win at 10 points; winningPlayer
       - pong-11: Sound effects (love.audio.newSource with 'static')
       - pong-12: love.resize callback; push:resize

    2. **For each version the user works on:**
       - Read the corresponding CS50G source (e.g., `course/01-pong/pong-N/main.lua`) when the user asks
       - Explain what changed from the previous version
       - Answer questions about the concepts
       - Help debug if the user's code doesn't work
       - Remind about require path adjustments (lib/ and src/ paths, assets/ paths)
       - Remind about pitfalls: dt-based movement, ball tunneling fix, color API (0-1 range), `canvas = false` in push:setupScreen

    3. **Key path adjustments the user must make** (vs CS50G originals):
       - `require 'lib/push'` not `require 'push'`
       - `require 'lib/class'` not `require 'class'`
       - `require 'src/Ball'` not `require 'Ball'`
       - `require 'src/Paddle'` not `require 'Paddle'`
       - `'assets/fonts/font.ttf'` not `'font.ttf'`
       - `'assets/sounds/paddle_hit.wav'` not `'sounds/paddle_hit.wav'`

    4. **After each version milestone**, encourage the user to:
       - Run `love projects/01-pong/` to verify it works
       - Update DEVLOG.md with their experience for that version
       - Git commit with message like `feat(pong): pong-N complete - [Version Name]`

    5. **Do NOT skip versions.** Each version teaches one concept. The user must work through all 13 sequentially per the locked decision.

    6. **When the user finishes pong-12 (resize)**, the game should be feature-complete: ball physics, two paddles with keyboard input, AABB collision, state machine (start/serve/play/done), scoring to 10, sound effects, and resize support.
  </action>
  <verify>
    <automated>test -f projects/01-pong/main.lua && test -f projects/01-pong/src/Ball.lua && test -f projects/01-pong/src/Paddle.lua && grep -q "gameState" projects/01-pong/main.lua && grep -q "collides" projects/01-pong/src/Ball.lua && echo "PASS: Core game files exist with expected patterns" || echo "FAIL: Missing files or patterns"</automated>
  </verify>
  <done>
    - main.lua contains full game loop with state machine (start, serve, play, done)
    - Ball.lua exists in src/ with collides() method and AABB detection
    - Paddle.lua exists in src/ with update() and boundary clamping
    - Game launches, paddles move with keyboard, ball bounces, score increments, win at 10
    - Sound effects play on paddle hit, wall hit, and score
    - Window resize works via push:resize
    - DEVLOG.md has entries for versions the user completed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Pong game runs correctly</name>
  <files>projects/01-pong/main.lua</files>
  <action>
    User plays through the complete Pong game to verify all mechanics work:
    1. Run: `love "C:\Users\Trynda\Desktop\Dev\Lua\projects\01-pong"`
    2. Press Enter to start — transitions from 'start' to 'serve'
    3. Press Enter again — ball launches
    4. Player 1 (left): W/S keys move paddle up/down
    5. Player 2 (right): Up/Down arrow keys move paddle up/down
    6. Verify ball bounces off paddles with sound effect
    7. Verify ball bouncing off top/bottom walls with sound effect
    8. Verify score increments when ball passes a paddle
    9. Play until one player reaches 10 — victory screen appears
    10. Press Enter on victory screen — game resets
    11. Resize the window — game scales correctly
    12. Press Escape — game quits
  </action>
  <verify>
    <automated>grep -q "gameState" projects/01-pong/main.lua && grep -q "collides" projects/01-pong/src/Ball.lua && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User confirmed all 12 checks pass: states work, paddles move, ball bounces, scoring works, win at 10, resize works</done>
</task>

</tasks>

<verification>
- Game launches from projects/01-pong/ without errors
- All 4 game states work (start, serve, play, done)
- Both paddles respond to keyboard input
- Ball collides with paddles and walls correctly
- Score displays and increments
- Win condition at 10 points works
- Sound effects play on events
- Window resize maintains correct scaling
- DEVLOG.md has been updated by the user
</verification>

<success_criteria>
- The user has studied and understood all 13 CS50G Pong versions (PONG-01)
- A complete, playable Pong game exists in projects/01-pong/ written from scratch (PONG-02)
- Game loop, input, collision, and scoring all work correctly (PONG-03)
- DEVLOG.md captures the user's rebuild experience and IDE feature ideas
</success_criteria>

<output>
After completion, create `.planning/phases/02-pong/02-02-SUMMARY.md`
</output>
