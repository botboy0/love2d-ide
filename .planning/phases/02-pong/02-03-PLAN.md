---
phase: 02-pong
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - projects/01-pong/main.lua
  - projects/01-pong/DEVLOG.md
autonomous: false
requirements:
  - PONG-04
  - PONG-05

must_haves:
  truths:
    - "AI paddle tracks ball Y position and deflects the ball during play state"
    - "Game is completable against the AI (AI is not unbeatable or the user has tuned it)"
    - "projects/01-pong/ follows the standard template layout (main.lua, conf.lua, lib/, src/, assets/)"
    - "Template pattern is documented and ready for reuse in future CS50G game rebuilds"
  artifacts:
    - path: "projects/01-pong/main.lua"
      provides: "Game with AI paddle logic replacing player2 keyboard input"
      contains: "player2.dy"
    - path: "projects/01-pong/DEVLOG.md"
      provides: "Complete dev log with AI implementation notes"
      min_lines: 60
  key_links:
    - from: "projects/01-pong/main.lua"
      to: "AI logic in love.update"
      via: "ball.y comparison to player2.y"
      pattern: "ball\\.y.*player2"
---

<objective>
Complete the CS50G "AI Update" assignment and finalize the standard project template.

Purpose: The AI paddle is the CS50G Pong assignment deliverable. Completing it proves the user understands the game well enough to modify its input handling. Finalizing the template establishes the pattern all future CS50G game rebuilds will follow.

Output: A Pong game playable against an AI opponent, with the project template pattern documented and proven.
</objective>

<execution_context>
@/home/botboy0/.claude/get-shit-done/workflows/execute-plan.md
@/home/botboy0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pong/02-CONTEXT.md
@.planning/phases/02-pong/02-RESEARCH.md
@.planning/phases/02-pong/02-02-SUMMARY.md

@projects/01-pong/main.lua
@projects/01-pong/src/Paddle.lua
@projects/01-pong/src/Ball.lua

<interfaces>
<!-- AI paddle pattern from RESEARCH.md (Pattern 5) -->
```lua
-- Replace player2 keyboard input section in love.update(dt):
if gameState == 'play' then
    if ball.y < player2.y then
        player2.dy = -PADDLE_SPEED
    elseif ball.y > player2.y + player2.height then
        player2.dy = PADDLE_SPEED
    else
        player2.dy = 0
    end
end
```

<!-- Assignment spec: "implement an AI-controlled paddle such that it will try to deflect the ball at all times" -->
<!-- "the paddle can move on only one axis (the Y axis)" -->
<!-- "each paddle has its own chunk of code where input is detected by the keyboard; this feels like an excellent place to put the code we need!" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assist user with AI paddle implementation and template finalization</name>
  <files>
    projects/01-pong/main.lua
    projects/01-pong/DEVLOG.md
  </files>
  <action>
    THIS IS A USER-DRIVEN CODING TASK. Claude assists, user writes.

    **Part A: AI Paddle (PONG-04)**

    1. Help the user read the CS50G assignment spec. The key requirements:
       - Replace keyboard input for one paddle (player2) with AI logic
       - AI moves paddle Y toward ball Y each frame
       - The code goes in the same place where keyboard input was (love.update)
       - Only during 'play' state

    2. Guide the user to implement:
       - In `love.update(dt)`, find the player2 keyboard section (`love.keyboard.isDown('up')` / `love.keyboard.isDown('down')`)
       - Replace it with ball-tracking logic: compare `ball.y` to `player2.y` and `player2.y + player2.height`
       - Set `player2.dy` to `-PADDLE_SPEED`, `PADDLE_SPEED`, or `0` accordingly

    3. Help test: the AI should move toward the ball and deflect it. At full PADDLE_SPEED, the AI may be very hard to beat. Per RESEARCH.md, a simple tracker at full speed is valid for the assignment spec. If the user wants to make it beatable, they can cap AI speed or add reaction delay — document the choice in DEVLOG.md.

    4. The user should also consider whether to add a mode toggle (1-player vs 2-player). The CONTEXT.md mentions "mode toggling" matching the assignment spec, but the fetched spec doesn't mandate it. If the user wants a toggle, help implement it (e.g., pressing 'a' in 'start' state toggles AI mode). If not, document the decision.

    **Part B: Template Finalization (PONG-05)**

    5. Verify the project structure matches the standard template:
       ```
       projects/01-pong/
       +-- main.lua
       +-- conf.lua
       +-- lib/
       |   +-- push.lua
       |   +-- class.lua
       +-- src/
       |   +-- Ball.lua
       |   +-- Paddle.lua
       +-- assets/
       |   +-- fonts/font.ttf
       |   +-- sounds/{paddle_hit,score,wall_hit}.wav
       +-- DEVLOG.md
       ```

    6. Ensure DEVLOG.md has a final section summarizing:
       - The template pattern for future games
       - Any IDE feature ideas collected during the rebuild
       - The AI implementation approach chosen

    7. Help the user make a final commit: `feat(pong): complete Pong with AI paddle - CS50G assignment done`
  </action>
  <verify>
    <automated>grep -q "ball\.y\|ball\.y" projects/01-pong/main.lua && grep -q "player2\.dy" projects/01-pong/main.lua && echo "PASS: AI paddle logic present" || echo "FAIL: AI paddle logic not found"</automated>
  </verify>
  <done>
    - AI paddle tracks ball and deflects it during play state
    - Game is completable (user can score against AI or AI demonstrates tracking)
    - Project structure matches standard template layout
    - DEVLOG.md has AI implementation notes and template summary
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AI paddle and template finalization</name>
  <files>projects/01-pong/main.lua</files>
  <action>
    User plays the game against the AI to verify the assignment is complete:
    1. Run: `love "C:\Users\Trynda\Desktop\Dev\Lua\projects\01-pong"`
    2. Start the game (Enter) and serve (Enter)
    3. Verify: Player 2 (right paddle) moves on its own toward the ball
    4. Verify: Player 1 still controlled by W/S keys
    5. Play a full game to 10 points — verify the game is completable
    6. Verify win screen appears and game can be restarted
    7. Check directory structure: `ls -R projects/01-pong/` matches template pattern
    8. Check DEVLOG.md has meaningful content from the rebuild experience
  </action>
  <verify>
    <automated>grep -q "player2.dy" projects/01-pong/main.lua && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User confirmed AI paddle works, game is completable, and template structure is correct</done>
</task>

</tasks>

<verification>
- AI paddle moves toward ball Y during play state
- Game is playable: human (player 1) vs AI (player 2)
- Game reaches victory state when a player scores 10
- Project directory follows standard template (main.lua, conf.lua, lib/, src/, assets/)
- DEVLOG.md has substantive entries from the rebuild process
</verification>

<success_criteria>
- CS50G "AI Update" assignment is complete (PONG-04)
- Standard project template is established and documented (PONG-05)
- The project in projects/01-pong/ can serve as the reference for future CS50G game rebuilds
</success_criteria>

<output>
After completion, create `.planning/phases/02-pong/02-03-SUMMARY.md`
</output>
