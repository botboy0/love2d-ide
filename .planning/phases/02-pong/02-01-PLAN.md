---
phase: 02-pong
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/01-pong/main.lua
  - projects/01-pong/conf.lua
  - projects/01-pong/lib/push.lua
  - projects/01-pong/lib/class.lua
  - projects/01-pong/assets/fonts/font.ttf
  - projects/01-pong/assets/sounds/paddle_hit.wav
  - projects/01-pong/assets/sounds/score.wav
  - projects/01-pong/assets/sounds/wall_hit.wav
  - projects/01-pong/DEVLOG.md
autonomous: false
requirements:
  - PONG-01
  - PONG-02
  - PONG-05

must_haves:
  truths:
    - "projects/01-pong/ directory exists with template structure (lib/, src/, assets/fonts/, assets/sounds/)"
    - "push.lua (patched) and class.lua are in lib/"
    - "CS50G font and sound assets are in assets/ subdirectories"
    - "DEVLOG.md exists with section headers for all 13 versions"
    - "A bare-minimum main.lua runs without errors via love.exe"
  artifacts:
    - path: "projects/01-pong/main.lua"
      provides: "Entry point — bare love.load/love.draw skeleton"
      min_lines: 10
    - path: "projects/01-pong/conf.lua"
      provides: "Window config — 1280x720, vsync=1, console=true"
      min_lines: 8
    - path: "projects/01-pong/lib/push.lua"
      provides: "Patched virtual resolution library"
    - path: "projects/01-pong/lib/class.lua"
      provides: "OOP library from hump/CS50G"
    - path: "projects/01-pong/DEVLOG.md"
      provides: "Dev log with version section stubs"
      min_lines: 30
  key_links:
    - from: "projects/01-pong/main.lua"
      to: "projects/01-pong/lib/push.lua"
      via: "require 'lib/push'"
      pattern: "require.*lib/push"
---

<objective>
Set up the Pong project directory and scaffold the incremental rebuild starting point.

Purpose: Create the working project from the template, copy all required assets and libraries, initialize DEVLOG.md, and produce a runnable pong-0 equivalent (bare window with text) that proves the project structure works. This gives the user a verified starting point for the incremental rebuild.

Output: A runnable `projects/01-pong/` project with all assets in place, DEVLOG.md initialized, and a pong-0-level main.lua that opens a window and displays text.
</objective>

<execution_context>
@/home/botboy0/.claude/get-shit-done/workflows/execute-plan.md
@/home/botboy0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pong/02-CONTEXT.md
@.planning/phases/02-pong/02-RESEARCH.md

@projects/_template/main.lua
@projects/_template/conf.lua
@course/01-pong/pong-final/class.lua
@course/01-pong/pong-0/main.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project directory and copy assets/libraries</name>
  <files>
    projects/01-pong/main.lua
    projects/01-pong/conf.lua
    projects/01-pong/lib/push.lua
    projects/01-pong/lib/class.lua
    projects/01-pong/assets/fonts/font.ttf
    projects/01-pong/assets/sounds/paddle_hit.wav
    projects/01-pong/assets/sounds/score.wav
    projects/01-pong/assets/sounds/wall_hit.wav
    projects/01-pong/src/.gitkeep
    projects/01-pong/DEVLOG.md
  </files>
  <action>
    1. Copy the template structure to create the project:
       - `cp -r projects/_template/* projects/01-pong/`
       - Ensure directories exist: `lib/`, `src/`, `assets/fonts/`, `assets/sounds/`

    2. Copy libraries:
       - push.lua is already in `projects/_template/lib/push.lua` (patched) — will come with template copy
       - Copy class.lua from CS50G reference: `cp course/01-pong/pong-final/class.lua projects/01-pong/lib/class.lua`

    3. Copy assets from CS50G reference:
       - Font: `cp course/01-pong/pong-final/font.ttf projects/01-pong/assets/fonts/font.ttf`
       - Sounds: `cp course/01-pong/pong-final/sounds/*.wav projects/01-pong/assets/sounds/`
         (paddle_hit.wav, score.wav, wall_hit.wav)

    4. Create `projects/01-pong/DEVLOG.md` with section stubs for all 13 versions plus pong-final:
       ```markdown
       # Pong Dev Log

       Development log for the CS50G Pong rebuild. Each section documents issues encountered
       and IDE feature ideas while studying and rebuilding that version.

       ## pong-0 — The Day-0 Update
       **Date:**
       **Concepts studied:** love.load, love.draw, love.graphics.printf
       **Issues encountered:**
       **IDE feature ideas:**

       ## pong-1 — The Low-Res Update
       ...
       (continue for all 13 versions through pong-12 and pong-final)
       ```

    5. Create `projects/01-pong/src/.gitkeep` so the empty src/ directory is tracked.

    6. Write `projects/01-pong/main.lua` as a pong-0 equivalent — the bare starting point:
       - `love.load()`: set window title to "Pong"
       - `love.draw()`: display "Hello Pong!" centered using `love.graphics.printf`
       - `love.keypressed(key)`: quit on escape
       - Do NOT add push.lua, classes, or any game logic yet — this is the pong-0 baseline
       - The user will incrementally add features themselves

    7. Keep `conf.lua` from template (1280x720, vsync=1, t.console=true) — it matches CS50G defaults.
  </action>
  <verify>
    <automated>ls projects/01-pong/main.lua projects/01-pong/conf.lua projects/01-pong/lib/push.lua projects/01-pong/lib/class.lua projects/01-pong/assets/fonts/font.ttf projects/01-pong/assets/sounds/paddle_hit.wav projects/01-pong/assets/sounds/score.wav projects/01-pong/assets/sounds/wall_hit.wav projects/01-pong/DEVLOG.md projects/01-pong/src/.gitkeep 2>&1 | grep -c "No such file" | grep -q "^0$" && echo "PASS: All files exist" || echo "FAIL: Missing files"</automated>
  </verify>
  <done>projects/01-pong/ has complete template structure with all libs, assets, and DEVLOG.md initialized</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify project scaffold runs on Windows</name>
  <files>projects/01-pong/main.lua</files>
  <action>
    User verifies the scaffold runs on Windows. Run `love "C:\Users\Trynda\Desktop\Dev\Lua\projects\01-pong"` and confirm:
    1. A 1280x720 window opens with "Hello Pong!" displayed centered
    2. Press Escape — window closes cleanly
    3. Directory structure has lib/, src/, assets/fonts/, assets/sounds/ with all files
    4. DEVLOG.md has section headers for all 13 versions
  </action>
  <verify>
    <automated>test -f projects/01-pong/main.lua && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User confirmed the game window opens and displays text correctly on Windows</done>
</task>

</tasks>

<verification>
- projects/01-pong/ directory exists with full template structure
- All libs (push.lua, class.lua) and assets (font.ttf, 3 wav files) are in correct locations
- main.lua runs via `love projects/01-pong/` and displays a window
- DEVLOG.md has section headers for all 13 CS50G versions
</verification>

<success_criteria>
- The project directory is ready for the user to begin the incremental rebuild
- Running `love projects/01-pong/` opens a window with "Hello Pong!" text
- All assets and libraries the user will need are already in place at the correct paths
- DEVLOG.md is initialized for the user to fill in as they work through each version
</success_criteria>

<output>
After completion, create `.planning/phases/02-pong/02-01-SUMMARY.md`
</output>
